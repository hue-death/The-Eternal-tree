```js
                if (challengeCompletions('a', 11) == 2) base = player.points.add(1).pow(0.225) // chal atom 1
                if (challengeCompletions('a', 12) == 2) base = player.p.points.add(1).pow(0.105) // chal atom 2
                if (challengeCompletions('a', 21) == 2) max = new Decimal(0.15) // chal atom 3
                if (challengeCompletions(this.layer, this.id) == 2) chalHardcap = new Decimal(0.25)// chal a 4 reward 1
            if (challengeCompletions(this.layer, this.id) == 2) HC = new Decimal(0.15) // chal p 1
            if (challengeCompletions('p', 22) == 2) base = player.points.add(1).pow(0.001875).div(2.5)//chal p 2
            if (challengeCompletions(this.layer, this.id) == 2) c22hc = new Decimal(4000)// chal p 2 rew 2
            if (challengeCompletions('p', 31) == 2) base = player.p.power.add(1).pow(0.01875).div(2.5)// chal p 3 rew 1
            if (challengeCompletions('p', 31) == 2) base = player.p.points.add(1).pow(0.0003).mul(1.2).mul(2)// chal p 3 rew 2
            if (challengeCompletions('p', 32) == 2) base = tmp.p.powerEff.pow(0.15)// chal p 4
```